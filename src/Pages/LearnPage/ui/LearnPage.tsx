import { LearnMode } from "@/features/LearnMode";

const LearnPage = () => {
    return (
        <div className="h-screen">
            <LearnMode />
        </div>
    );
};

export default LearnPage;