import { EditMode } from "@/features/EditMode";

const EditPage = () => {
    return (
        <div className="h-full">
            <EditMode type="new" />
        </div>
    );
};

export default EditPage;