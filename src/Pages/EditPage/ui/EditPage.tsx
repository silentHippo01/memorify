import { EditMode } from "@/features/EditMode";

const EditPage = () => {
    return (
        <div className="h-full h-screen">
            <EditMode type="new" />
        </div>
    );
};

export default EditPage;